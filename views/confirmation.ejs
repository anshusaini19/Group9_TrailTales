<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Booking Confirmation</title>
  <link rel="stylesheet" href="/css/confirmation.css"> <!-- optional, if you have a separate CSS file -->
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f3f4f6;
      margin: 0;
      padding: 0;
    }
    .confirmation-container {
      max-width: 600px;
      margin: 80px auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      text-align: center;
    }
    h2 {
      color: #28a745;
    }
    p {
      font-size: 1.1rem;
      margin: 10px 0;
    }
    .back-btn {
      margin-top: 20px;
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
    }
    .back-btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="confirmation-container">
    <h2>🎉 Booking Confirmed!</h2>
    <p>Thank you, <strong><%= booking.name %></strong>.</p>
    <p>Your booking for <strong><%= package?.title || 'the selected package' %></strong> has been confirmed.</p>
    <p>A confirmation email has been sent to <strong><%= booking.email %></strong>.</p>
    <p>Booking ID: <code><%= booking._id %></code></p>
    <a href="/api/home" class="back-btn">Back to Home</a>
  </div>
</body>
</html>
