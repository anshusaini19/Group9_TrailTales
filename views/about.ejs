<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us - TrailTales</title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/about.css">
</head>
<body>

    <!-- Header -->
    <header class="d-flex justify-content-between align-items-center p-3 shadow-sm">
        <div class="logo font-weight-bold h4 mb-0">TrailTales</div>
        <nav class="d-flex gap-3">
            <a href="/api/home">Home</a>
            <a href="/api/about">About Us</a>
            <a href="/api/destinations">Destinations</a>
            <a href="/api/gallery">Gallery</a>
            <a href="/api/testimonials">Testimonials</a>
            <a href="/api/contact">Contact Us</a>
            <a href="/api/book">Book Now</a>
        </nav>
    </header>

    <!-- About Us Section -->
    <section class="about-section py-5" id="about">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-4 font-weight-bold">Meet Our Team</h2>
                <p class="lead text-muted">Our passionate team is dedicated to helping you plan the best travel experiences. Get to know the faces behind TrailTales.</p>
            </div>

            <div class="row">
                <% if (team && team.length > 0) { %>
                    <% team.forEach(member => { %>
                        <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                            <div class="card team-member h-100 shadow-sm border-0">
                                <img class="card-img-top" src="/Images/<%= member.image %>" alt="<%= member.name %>">
                                <div class="card-body text-center">
                                    <h5 class="card-title font-weight-bold"><%= member.name %></h5>
                                    <p class="card-subtitle text-muted"><%= member.role %></p>
                                    <p class="card-text mt-2"><%= member.bio %></p>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12 text-center">
                        <p class="text-danger">No team members found. Please check the data source.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-light py-3 text-center">
        <p class="mb-0 text-muted">&copy; 2024 TrailTales. All rights reserved. | <a href="#">Privacy Policy</a></p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
